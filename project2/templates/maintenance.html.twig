<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ status }} {{ phrase(status, true) }}</title>
        <link rel="stylesheet" href="{{ asset('adminlte3/css/' ~ cssfile('adminlte.css')) }}">
        <link rel="stylesheet" href="{{ asset('plugins/fontawesome-free/css/all.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css/' ~ cssfile(config('PROJECT_STYLESHEET_FILENAME'))) }}">
    </head>
    <body class="container-fluid">
        <div class="d-flex justify-content-center align-items-center h-100">
            <div class="error-page">
                <div class="d-flex flex-row align-items-center mb-3">
                    <div class="p-3">
                        <h2 class="headline text-danger">{{ status }}</h2>
                    </div>
                    <div class="p-3 error-content">
                        <h3><i class="fa-solid fa-triangle-exclamation text-danger"></i> {{ title }}</h3>
                        <p>{{ description }}</p>
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>